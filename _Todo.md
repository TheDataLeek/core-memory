# Global Todos

All open tasks across the vault, grouped by folder.

```tasks
not done
group by folder
sort by priority
sort by due
```

---

## Waiting / Blocked

```tasks
not done
(tag includes #waiting) OR (tag includes #blocked)
sort by priority
sort by due
```
